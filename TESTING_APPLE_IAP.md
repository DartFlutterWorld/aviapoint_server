# Как протестировать оплату через Apple IAP

## 📱 Подготовка к тестированию

### 1. Создание Sandbox Test Account

1. Войдите в [App Store Connect](https://appstoreconnect.apple.com)
2. Перейдите: **Users and Access** → **Sandbox Testers**
3. Нажмите **"+"** для создания нового тестового аккаунта
4. Заполните данные:
   - **Email:** используйте уникальный email (например: `test.aviapoint+1@gmail.com`)
   - **Password:** придумайте пароль
   - **First Name:** Test
   - **Last Name:** User
   - **Country/Region:** Россия
5. Нажмите **"Save"**

⚠️ **Важно:** 
- Email должен быть уникальным и не использоваться в реальном Apple ID
- Пароль должен соответствовать требованиям Apple (минимум 8 символов, заглавные и строчные буквы, цифры)

### 2. Настройка устройства для тестирования

1. **Выйдите из App Store на устройстве:**
   - Настройки → [Ваше имя] → Медиа и покупки → Выйти

2. **Убедитесь, что приложение установлено:**
   - Соберите и установите приложение на реальное iOS устройство
   - ⚠️ **Важно:** IAP не работает на симуляторе!

### 3. Проверка настроек в Xcode

1. Откройте проект в Xcode
2. Выберите Target → Signing & Capabilities
3. Убедитесь, что включен **In-App Purchase** capability
4. Проверьте, что Bundle ID совпадает: `ru.dartflutter.aviapoint`

---

## 🧪 Процесс тестирования

### Шаг 1: Запуск приложения

1. Запустите приложение на реальном iOS устройстве
2. Войдите в приложение (или зарегистрируйтесь)
3. Перейдите в раздел, где можно купить подписку:
   - **Экран выбора режима тестирования** → "Тренировочный режим"
   - **Экран профиля** → раздел подписки

### Шаг 2: Инициация покупки

1. Нажмите на кнопку покупки подписки
2. Должен появиться диалог Apple для входа в Sandbox

### Шаг 3: Вход в Sandbox

1. В диалоге Apple введите **Sandbox Test Account**:
   - Email: тот, который создали в App Store Connect
   - Password: пароль, который указали
2. Нажмите **"Войти"**

⚠️ **Важно:** 
- Это НЕ ваш реальный Apple ID
- Это специальный тестовый аккаунт для Sandbox
- Покупки в Sandbox бесплатны и не списывают реальные деньги

### Шаг 4: Подтверждение покупки

1. После входа появится диалог подтверждения покупки
2. Нажмите **"Купить"** или **"Подтвердить"**
3. Покупка будет обработана через Sandbox

### Шаг 5: Проверка результата

1. **В приложении:**
   - Должно появиться сообщение "Подписка успешно активирована"
   - Подписка должна стать активной
   - Доступ к платному контенту должен открыться

2. **На сервере:**
   - Проверьте логи: `docker logs aviapoint-server --tail=100 | grep -i "iap\|apple"`
   - Проверьте БД:
     ```sql
     SELECT * FROM payments WHERE payment_source = 'apple_iap' ORDER BY created_at DESC LIMIT 1;
     SELECT * FROM subscriptions WHERE user_id = <user_id> ORDER BY created_at DESC LIMIT 1;
     ```

---

## 🔍 Проверка работы

### Проверка в App Store Connect

1. Перейдите: **Sales and Trends** → **In-App Purchases**
2. Выберите вашу подписку
3. Проверьте транзакции в Sandbox

### Проверка логов сервера

```bash
# На сервере
docker logs aviapoint-server --tail=200 | grep -i "verify-iap\|iap\|apple"
```

Должны увидеть:
- Запрос на `/api/payments/verify-iap`
- Успешную верификацию транзакции
- Создание платежа в БД
- Активацию подписки

### Проверка в базе данных

```sql
-- Проверка платежа
SELECT 
  id, 
  status, 
  payment_source, 
  apple_transaction_id,
  created_at 
FROM payments 
WHERE payment_source = 'apple_iap' 
ORDER BY created_at DESC 
LIMIT 5;

-- Проверка подписки
SELECT 
  id,
  user_id,
  subscription_type,
  start_date,
  end_date,
  is_active
FROM subscriptions 
WHERE user_id = <user_id>
ORDER BY created_at DESC 
LIMIT 5;
```

---

## ⚠️ Частые проблемы и решения

### Проблема 1: "In-App Purchases недоступны"

**Причина:** Приложение запущено на симуляторе или IAP не настроен

**Решение:**
- Используйте реальное iOS устройство
- Проверьте capabilities в Xcode
- Убедитесь, что Bundle ID правильный

### Проблема 2: "Не удалось загрузить продукты"

**Причина:** Product ID не совпадает или подписка не создана в App Store Connect

**Решение:**
- Проверьте Product ID: `com.aviapoint.subscription.rosaviatest.yearly`
- Убедитесь, что подписка создана и имеет статус "Готово к отправке"
- Проверьте Bundle ID в App Store Connect

### Проблема 3: "Ошибка верификации на сервере"

**Причина:** Проблемы с API ключами или сервером

**Решение:**
- Проверьте переменные окружения на сервере
- Проверьте логи сервера на ошибки
- Убедитесь, что миграция 073 применена

### Проблема 4: Диалог Apple не появляется

**Причина:** Уже вошли в App Store с реальным Apple ID

**Решение:**
- Выйдите из App Store: Настройки → [Ваше имя] → Медиа и покупки → Выйти
- Перезапустите приложение

---

## 📝 Чек-лист перед тестированием

- [ ] Sandbox Test Account создан в App Store Connect
- [ ] Приложение установлено на реальное iOS устройство (не симулятор)
- [ ] Вышли из App Store на устройстве
- [ ] Bundle ID совпадает: `ru.dartflutter.aviapoint`
- [ ] Product ID совпадает: `com.aviapoint.subscription.rosaviatest.yearly`
- [ ] Подписка создана в App Store Connect со статусом "Готово к отправке"
- [ ] API ключи настроены на сервере
- [ ] Миграция 073 применена на сервере
- [ ] Переменные окружения установлены на сервере

---

## 🎯 Ожидаемый результат

После успешной покупки:

1. ✅ Диалог Apple показывает успешную покупку
2. ✅ Приложение показывает "Подписка успешно активирована"
3. ✅ Подписка активна в профиле
4. ✅ Доступ к платному контенту открыт
5. ✅ В БД создан платеж с `payment_source = 'apple_iap'`
6. ✅ В БД создана/обновлена подписка
7. ✅ Логи сервера показывают успешную верификацию

---

## 🔄 Восстановление покупок

Для тестирования восстановления покупок:

1. Удалите приложение с устройства
2. Переустановите приложение
3. Войдите с тем же Sandbox Test Account
4. Используйте функцию "Восстановить покупки" (если реализована)

---

## 📞 Если что-то не работает

1. Проверьте логи приложения в Xcode Console
2. Проверьте логи сервера
3. Проверьте статус подписки в App Store Connect
4. Убедитесь, что используете Sandbox Test Account, а не реальный Apple ID

---

**Удачи с тестированием! 🚀**
