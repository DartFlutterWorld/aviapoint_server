# 🚀 Развертывание новых миграций на продакшн

## Автоматический способ (рекомендуется)

### Шаг 1: Обновить код на сервере

```bash
# Подключиться к серверу
ssh root@83.166.246.205

# Перейти в папку проекта
cd /home/aviapoint_server

# Получить последние изменения
git pull
```

### Шаг 2: Пересобрать и перезапустить сервер

```bash
# На сервере
docker-compose -f docker-compose.prod.yaml build app
docker-compose -f docker-compose.prod.yaml up -d app
```

**Миграции применятся автоматически** при запуске сервера через `MigrationManager`.

### Шаг 3: Проверить логи

```bash
# Проверить, что миграции применились
docker logs -f aviapoint-server | grep -i migration
```

Или проверить в БД:

```bash
docker exec aviapoint-postgres psql -U postgres -d aviapoint -c "SELECT version, name, executed_at FROM schema_migrations ORDER BY executed_at DESC LIMIT 10;"
```

---

## Ручной способ (через Adminer)

Если нужно применить миграции вручную:

### Шаг 1: Открыть Adminer

```
http://83.166.246.205:8082
```

### Шаг 2: Войти в базу данных

- Система: `PostgreSQL`
- Сервер: `aviapoint-postgres`
- Пользователь: `postgres`
- Пароль: (из переменных окружения)
- База данных: `aviapoint`

### Шаг 3: Применить миграции

1. Перейдите в раздел **"SQL-запрос"**
2. Скопируйте содержимое файлов миграций из папки `migrations/` в порядке версий:
   - `022_create_airport_visitor_photos_table.sql`
   - `023_add_visitor_photos_to_airports.sql`
   - `024_create_flight_waypoints_table.sql`
   - `025_clear_all_flights_data.sql` ⚠️ **Удаляет данные!**
   - `026_create_flight_questions_table.sql`
   - `027_remove_subscription_fields_from_profiles.sql`
   - `028_remove_unique_active_subscription_index.sql`
   - `029_add_telegram_and_max_to_profiles.sql`
3. Выполните каждую миграцию по очереди

---

## Использование скрипта

```bash
# Сделать скрипт исполняемым
chmod +x deploy_migrations_to_prod.sh

# Запустить скрипт
./deploy_migrations_to_prod.sh
```

Или с указанием IP:

```bash
./deploy_migrations_to_prod.sh 83.166.246.205
```

---

## Список новых миграций

После последнего развертывания были добавлены следующие миграции:

| Версия | Название | Описание |
|--------|----------|----------|
| 022 | `create_airport_visitor_photos_table` | Создает таблицу для фотографий посетителей аэропортов |
| 023 | `add_visitor_photos_to_airports` | Добавляет поле `visitor_photos` в таблицу `airports` |
| 024 | `create_flight_waypoints_table` | Создает таблицу для точек маршрута полета |
| 025 | `clear_all_flights_data` | ⚠️ **Удаляет все данные о полетах** |
| 026 | `create_flight_questions_table` | Создает таблицу для вопросов о полете |
| 027 | `remove_subscription_fields_from_profiles` | Удаляет поля подписки из таблицы `profiles` |
| 028 | `remove_unique_active_subscription_index` | Удаляет уникальный индекс для подписок |
| 029 | `add_telegram_and_max_to_profiles` | Добавляет поля `telegram` и `max` в таблицу `profiles` |

---

## Проверка после развертывания

### Проверить статус миграций

```bash
ssh root@83.166.246.205
docker exec aviapoint-postgres psql -U postgres -d aviapoint -c "SELECT version, name, executed_at FROM schema_migrations ORDER BY executed_at DESC;"
```

### Проверить новые таблицы

```bash
docker exec aviapoint-postgres psql -U postgres -d aviapoint -c "\dt" | grep -E "airport_visitor_photos|flight_waypoints|flight_questions"
```

### Проверить новые поля

```bash
# Проверить поля telegram и max в profiles
docker exec aviapoint-postgres psql -U postgres -d aviapoint -c "\d profiles" | grep -E "telegram|max"
```

---

## Важные замечания

⚠️ **Миграция 025 (`clear_all_flights_data`)** удаляет все данные о полетах:
- Все полеты
- Все бронирования
- Все отзывы
- Все фотографии полетов
- Все точки маршрута

**Не применяйте эту миграцию на продакшн, если нужно сохранить данные!**

Если нужно пропустить эту миграцию:
1. Удалите её из `MigrationManager` перед развертыванием
2. Или вручную добавьте запись в `schema_migrations`, чтобы она считалась выполненной

---

## Откат миграций

Если что-то пошло не так, можно откатить последнюю миграцию:

```bash
ssh root@83.166.246.205
docker exec aviapoint-postgres psql -U postgres -d aviapoint -c "DELETE FROM schema_migrations WHERE version = '029';"
```

Затем перезапустите сервер для повторного применения миграций.

