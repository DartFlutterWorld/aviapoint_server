# 🔄 Синхронизация структуры БД (локальная -> продакшн)

## Описание

Скрипт синхронизирует **только структуру** БД (схему) с локальной на продакшн:
- ✅ Создает новые таблицы
- ✅ Добавляет новые поля в существующие таблицы
- ✅ Создает новые индексы и ограничения
- ❌ **НЕ переносит данные** (данные на продакшн остаются нетронутыми)
- ❌ **НЕ трогает** таблицу `schema_migrations`

## Использование

```bash
# Сделать скрипт исполняемым
chmod +x sync_schema_local_to_prod.sh

# Запустить скрипт
./sync_schema_local_to_prod.sh 83.166.246.205
```

Или без указания IP (используется по умолчанию):

```bash
./sync_schema_local_to_prod.sh
```

## Что делает скрипт

1. **Экспортирует структуру** с локальной БД (только схему, без данных)
2. **Копирует файл** на продакшн сервер
3. **Применяет структуру** на продакшн БД
4. **Игнорирует ошибки** для уже существующих объектов (например, если таблица уже есть)

## Безопасность

✅ **Данные не затрагиваются** - скрипт использует `--schema-only`, что означает только структуру

✅ **Таблица `schema_migrations` не трогается** - она исключена из экспорта

✅ **Игнорируются ошибки** для существующих объектов - если таблица уже есть, она не будет пересоздана

## Пример использования

```bash
# 1. Убедитесь, что локальная БД содержит нужную структуру
docker exec aviapoint-postgres psql -U postgres -d aviapoint -c "\dt"

# 2. Запустите синхронизацию
./sync_schema_local_to_prod.sh

# 3. Проверьте результат на продакшн
ssh root@83.166.246.205
docker exec aviapoint-postgres psql -U postgres -d aviapoint -c "\dt"
```

## Ручной способ

Если нужно применить структуру вручную:

```bash
# 1. Экспорт структуры локально
docker exec aviapoint-postgres pg_dump -U postgres -d aviapoint \
  --schema-only \
  --no-owner \
  --no-privileges \
  --exclude-table=schema_migrations > schema_export.sql

# 2. Копирование на сервер
scp schema_export.sql root@83.166.246.205:/tmp/

# 3. Применение на сервере
ssh root@83.166.246.205
cat /tmp/schema_export.sql | docker exec -i aviapoint-postgres psql -U postgres -d aviapoint
```

## Важные замечания

⚠️ **Это изменит структуру БД на продакшн!** Убедитесь, что:
- Локальная структура БД актуальна
- Все миграции применены локально
- Вы понимаете, какие изменения будут применены

✅ **Данные останутся нетронутыми** - скрипт не удаляет и не изменяет существующие данные

✅ **Безопасно для продакшн** - если объект уже существует, он не будет пересоздан

## Откат изменений

Если что-то пошло не так, можно откатить изменения через миграции или восстановить из резервной копии:

```bash
# Создать резервную копию структуры перед синхронизацией
ssh root@83.166.246.205
docker exec aviapoint-postgres pg_dump -U postgres -d aviapoint \
  --schema-only > /tmp/schema_backup_$(date +%Y%m%d_%H%M%S).sql
```


