# 🔄 Обновление серверного приложения после изменений

## Когда нужно перезапускать контейнеры

### ✅ Нужно перезапустить:

1. **Изменился код Dart** (lib/*.dart)
   - Нужно пересобрать образ и перезапустить контейнер `app`

2. **Изменился Dockerfile**
   - Нужно пересобрать образ

3. **Изменился docker-compose.prod.yaml**
   - Нужно перезапустить все контейнеры

4. **Изменился .env файл**
   - Нужно перезапустить контейнер `app`

### ❌ НЕ нужно перезапускать:

1. **Изменились данные в БД**
   - БД обновляется автоматически

2. **Изменились статические файлы в public/**
   - Обычно не нужно, но может потребоваться перезагрузка nginx

3. **Изменился nginx.conf**
   - Достаточно перезагрузить nginx (не перезапускать контейнер)

---

## Процесс обновления после изменения кода

### Вариант 1: Быстрое обновление (рекомендуется)

```bash
# На сервере
cd /home/aviapoint_server

# 1. Обновите код (если используете git)
git pull origin main

# 2. Пересоберите образ приложения
docker-compose -f docker-compose.prod.yaml build app

# 3. Перезапустите контейнер приложения
docker-compose -f docker-compose.prod.yaml up -d --no-deps app

# 4. Проверьте логи
docker-compose -f docker-compose.prod.yaml logs -f app
```

### Вариант 2: Полная пересборка

```bash
# На сервере
cd /home/aviapoint_server

# 1. Обновите код
git pull origin main

# 2. Остановите контейнер приложения
docker-compose -f docker-compose.prod.yaml stop app

# 3. Пересоберите образ (без кеша для чистой сборки)
docker-compose -f docker-compose.prod.yaml build --no-cache app

# 4. Запустите контейнер
docker-compose -f docker-compose.prod.yaml up -d app

# 5. Проверьте статус
docker-compose -f docker-compose.prod.yaml ps
```

### Вариант 3: Перезапуск всех контейнеров

```bash
# На сервере
cd /home/aviapoint_server

# 1. Обновите код
git pull origin main

# 2. Пересоберите и перезапустите все
docker-compose -f docker-compose.prod.yaml up -d --build

# 3. Проверьте статус
docker-compose -f docker-compose.prod.yaml ps
```

---

## Обновление после изменения конфигурации

### Изменился .env файл

```bash
# На сервере
cd /home/aviapoint_server

# 1. Отредактируйте .env
nano .env

# 2. Перезапустите контейнер приложения
docker-compose -f docker-compose.prod.yaml restart app

# Или пересоздайте контейнер (чтобы точно применить изменения)
docker-compose -f docker-compose.prod.yaml up -d --force-recreate app
```

### Изменился nginx.conf

```bash
# На сервере
cd /home/aviapoint_server

# 1. Отредактируйте nginx.conf
nano nginx.conf

# 2. Проверьте конфигурацию
docker exec aviapoint-nginx nginx -t

# 3. Перезагрузите nginx (не перезапускайте контейнер!)
docker exec aviapoint-nginx nginx -s reload
```

### Изменились статические файлы в public/

```bash
# Обычно не нужно ничего делать
# Но если файлы не обновляются, перезагрузите nginx:
docker exec aviapoint-nginx nginx -s reload
```

---

## Проверка после обновления

### 1. Проверьте статус контейнеров

```bash
docker-compose -f docker-compose.prod.yaml ps
```

Все контейнеры должны быть в статусе `Up` или `Up (healthy)`

### 2. Проверьте логи

```bash
# Логи приложения
docker-compose -f docker-compose.prod.yaml logs --tail=50 app

# Ищите ошибки или подтверждение успешного запуска
```

### 3. Проверьте API

```bash
# Проверьте, что API отвечает
curl http://localhost:8080/openapi/

# Или через nginx
curl http://localhost/openapi/
```

---

## Быстрые команды

### Перезапуск только приложения

```bash
docker-compose -f docker-compose.prod.yaml restart app
```

### Пересборка и перезапуск приложения

```bash
docker-compose -f docker-compose.prod.yaml up -d --build app
```

### Перезапуск всех сервисов

```bash
docker-compose -f docker-compose.prod.yaml restart
```

### Полная пересборка всего

```bash
docker-compose -f docker-compose.prod.yaml down
docker-compose -f docker-compose.prod.yaml build --no-cache
docker-compose -f docker-compose.prod.yaml up -d
```

---

## Типичные сценарии

### Сценарий 1: Изменил код в lib/main.dart

```bash
cd /home/aviapoint_server
git pull  # или скопируйте файлы вручную
docker-compose -f docker-compose.prod.yaml build app
docker-compose -f docker-compose.prod.yaml up -d app
docker-compose -f docker-compose.prod.yaml logs -f app
```

### Сценарий 2: Изменил переменные в .env

```bash
cd /home/aviapoint_server
nano .env  # отредактируйте
docker-compose -f docker-compose.prod.yaml up -d --force-recreate app
```

### Сценарий 3: Изменил конфигурацию nginx

```bash
cd /home/aviapoint_server
nano nginx.conf  # отредактируйте
docker exec aviapoint-nginx nginx -t  # проверьте
docker exec aviapoint-nginx nginx -s reload  # перезагрузите
```

---

## Важно!

- ⚠️ **БД не нужно перезапускать** при изменении кода приложения
- ⚠️ **Nginx не нужно перезапускать** - достаточно перезагрузки (`nginx -s reload`)
- ✅ **Всегда проверяйте логи** после обновления
- ✅ **Проверяйте работоспособность API** после обновления

---

## Автоматизация (опционально)

Можно создать скрипт для быстрого обновления:

```bash
#!/bin/bash
# update_app.sh
cd /home/aviapoint_server
git pull
docker-compose -f docker-compose.prod.yaml build app
docker-compose -f docker-compose.prod.yaml up -d app
docker-compose -f docker-compose.prod.yaml logs -f app
```

Использование:
```bash
./update_app.sh
```

---

**Готово!** Теперь вы знаете, когда и как обновлять приложение! 🚀

