# Настройка тестовых ключей ЮKassa

## Ваши тестовые ключи

Из вашего тестового магазина:
- **Shop ID:** `1263563`
- **Secret Key:** `test_smWzm-HQP7WiixTzc5hnBtHv3toaasT3_BLbaouMhrY`

## Настройка для локальной разработки

1. Откройте или создайте файл `.env.local` в корне проекта `aviapoint_server`:

```bash
cd /Users/admin/Projects/aviapoint_server
nano .env.local
```

2. Добавьте следующие строки:

```bash
# ЮKassa - ТЕСТОВЫЙ режим
YOOKASSA_TEST_MODE=true
YOOKASSA_SHOP_ID=1263563
YOOKASSA_SECRET_KEY=test_smWzm-HQP7WiixTzc5hnBtHv3toaasT3_BLbaouMhrY
```

3. Сохраните файл (Ctrl+O, Enter, Ctrl+X в nano)

4. Запустите сервер:

```bash
./run_local.sh
```

Или вручную:
```bash
ENVIRONMENT=local dart run
```

5. Проверьте логи - должно появиться:
```
🔧 ЮKassa: ТЕСТОВЫЙ режим включен
```

## Важно: Тестирование только локально

⚠️ **Тестовые ключи используются ТОЛЬКО для локальной разработки!**

- `docker-compose.dev.yaml` - для локальной разработки (БД + Adminer)
- `docker-compose.prod.yaml` - для продакшн сервера (всегда использует продакшн ключи)

**НЕ используйте тестовые ключи на продакшн сервере!** Продакшн сервер всегда должен использовать продакшн ключи ЮKassa.

## Тестирование платежей

После настройки используйте тестовые карты:

### Успешная оплата:
- **Номер карты:** `5555 5555 5555 4444`
- **Срок действия:** любая будущая дата (например, `12/25`)
- **CVC:** любые 3 цифры (например, `123`)
- **Имя держателя:** любое

### Отмена платежа:
- **Номер карты:** `5555 5555 5555 4477`

### Ошибка платежа:
- **Номер карты:** `5555 5555 5555 4499`

## Возврат в продакшн режим (для локальной разработки)

Если нужно протестировать продакшн ключи локально:

1. В `.env.local` установите:
```bash
YOOKASSA_TEST_MODE=false
YOOKASSA_SHOP_ID=1214860
YOOKASSA_SECRET_KEY=live_A8iyj3kBLfq4YUiKwlHoPpvBP0B7BQIBhY3vOPuDisc
```

2. Перезапустите сервер

3. В логах должно появиться:
```
🔧 ЮKassa: ПРОДАКШН режим
```

**Примечание:** Продакшн сервер (`docker-compose.prod.yaml`) всегда использует продакшн ключи, переключение не требуется.
