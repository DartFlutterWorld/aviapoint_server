# Инструкция по настройке загрузки фото профиля

## ✅ Что реализовано

1. **База данных**: Добавлено поле `avatar_url` в таблицу `profiles`
2. **Backend API**: Создан endpoint `POST /api/profile/photo` для загрузки фото
3. **Frontend**: Добавлен UI для выбора и загрузки фото в профиле

## 📋 Шаги для применения

### 1. Применить миграцию БД

Выполните SQL миграцию:

```sql
ALTER TABLE profiles ADD COLUMN IF NOT EXISTS avatar_url VARCHAR(255);
```

Или используйте файл миграции:
```bash
psql -U postgres -d your_database -f migrations/add_avatar_url_to_profiles.sql
```

### 2. Создать директорию для фото

На бэкенде создайте директорию для хранения фото:

```bash
mkdir -p public/profiles
```

### 3. Запустить build_runner на бэкенде (если нужно)

Если нужно перегенерировать код для ProfileModel:

```bash
cd /Users/admin/Projects/aviapoint_server
dart pub run build_runner build --delete-conflicting-outputs
```

### 4. Перезапустить бэкенд сервер

После применения миграции и создания директории перезапустите сервер.

## 🎯 Как использовать

1. **Пользователь открывает профиль** → нажимает "Изменить данные"
2. **В bottom sheet** видит свое текущее фото (или дефолтное `Pictures.pilot`)
3. **Нажимает на фото** → открывается галерея для выбора фото
4. **Выбирает фото** → фото отображается в preview
5. **Нажимает "Сохранить"** → фото загружается на сервер
6. **После успешной загрузки** → обновляются остальные данные профиля

## 📁 Структура файлов

- **Фото хранятся**: `public/profiles/{userId}.{extension}`
- **В БД сохраняется**: `profiles/{userId}.{extension}`
- **URL для доступа**: `/public/profiles/{userId}.{extension}`

## 🔒 Безопасность

- ✅ Проверка авторизации (только авторизованные пользователи)
- ✅ Проверка прав (только свой профиль)
- ✅ Валидация размера файла (максимум 5MB)
- ✅ Валидация формата (JPG/PNG)
- ✅ Автоматическое удаление старого фото при загрузке нового

## 🐛 Отладка

Если фото не загружается:

1. Проверьте, что директория `public/profiles` существует
2. Проверьте права доступа к директории
3. Проверьте логи бэкенда на наличие ошибок
4. Проверьте, что миграция БД применена
5. Проверьте, что `avatar_url` правильно сохраняется в БД

## 📝 Примечания

- При загрузке нового фото старое автоматически удаляется
- Если фото нет, показывается дефолтное изображение `Pictures.pilot`
- Фото автоматически ресайзится на клиенте до 800x800px перед загрузкой
- Качество сжатия: 85%

