# ⚡ Быстрая справка

## 🎯 Вы выбираете хостинг за 30 секунд

```
╔═══════════════════════════════════════════════════════════╗
║          ВЫБЕРИТЕ ВАШ ВАРИАНТ ХОСТИНГА                   ║
╚═══════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────┐
│ ⭐ САМЫЙ ПРОСТОЙ: Render.com                            │
├─────────────────────────────────────────────────────────┤
│ 1. render.com → New → PostgreSQL                        │
│ 2. render.com → New → Web Service (GitHub)              │
│ 3. Установить env переменные                            │
│ 4. Deploy! ✅                                           │
├─────────────────────────────────────────────────────────┤
│ ⏱️  Время: 5 минут                                      │
│ 💰 Цена: $7-25/мес                                      │
│ 🔧 Сложность: ⭐ (Easy)                                │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ 🏢 СБАЛАНСИРОВАННЫЙ: DigitalOcean App Platform          │
├─────────────────────────────────────────────────────────┤
│ 1. digitalocean.com → App Platform → Create App         │
│ 2. Подключить GitHub репо                              │
│ 3. Указать build & start команды                        │
│ 4. Deploy! ✅                                           │
├─────────────────────────────────────────────────────────┤
│ ⏱️  Время: 10 минут                                     │
│ 💰 Цена: $12-40/мес                                     │
│ 🔧 Сложность: ⭐ (Easy)                                │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ 🔧 ПОЛНЫЙ КОНТРОЛЬ: VPS + Docker                        │
├─────────────────────────────────────────────────────────┤
│ 1. Купить VPS (Linode/Vultr/Hetzner)                    │
│ 2. SSH подключиться                                     │
│ 3. ./start.sh production                                │
│ 4. Готово! ✅                                           │
├─────────────────────────────────────────────────────────┤
│ ⏱️  Время: 30 минут                                     │
│ 💰 Цена: $5-20/мес (САМОЕ ДЕШЕВОЕ!)                    │
│ 🔧 Сложность: ⭐⭐⭐ (Hard)                            │
└─────────────────────────────────────────────────────────┘
```

---

## 📋 Команды по категориям

### 🐳 Docker Основы

```bash
# Статус контейнеров
docker ps
docker-compose ps

# Запуск/Остановка
docker-compose up -d
docker-compose down

# Логи
docker logs CONTAINER_ID
docker-compose logs app -f
docker-compose logs db -f

# Перезагрузка
docker restart CONTAINER_ID
docker-compose restart
```

### 🔍 Диагностика

```bash
# Общее здоровье
docker-compose ps
docker-compose config
docker system df

# Статистика ресурсов
docker stats
free -h              # Память
df -h                # Диск

# Проверить порты
netstat -tuln | grep 80
netstat -tuln | grep 443

# Проверить сеть
docker network ls
docker network inspect aviapoint_server_backend
```

### 🗄️ PostgreSQL

```bash
# Вход в БД
docker exec -it aviapoint-postgres psql -U postgres

# SQL команда
docker exec -it aviapoint-postgres psql -U postgres -d aviapoint -c "SELECT 1"

# Бэкап
docker exec aviapoint-postgres pg_dump -U postgres aviapoint > backup.sql

# Восстановление
cat backup.sql | docker exec -i aviapoint-postgres psql -U postgres -d aviapoint

# Размер БД
docker exec aviapoint-postgres du -sh /var/lib/postgresql/data
```

### 🔐 SSL Сертификаты

```bash
# Получить сертификат
certbot certonly --standalone -d yourdomain.com

# Копировать в проект
cp /etc/letsencrypt/live/yourdomain.com/fullchain.pem ssl/cert.pem
cp /etc/letsencrypt/live/yourdomain.com/privkey.pem ssl/key.pem

# Проверить сертификат
openssl x509 -in ssl/cert.pem -text -noout

# Автоматическое обновление
sudo crontab -e
# Добавить: 0 0 1 * * certbot renew --quiet
```

### 🚀 Deploy & Обновления

```bash
# Скопировать и отредактировать config
cp env.example .env
nano .env

# Первый запуск
./start.sh production

# Обновить код
git pull origin main

# Пересобрать образ
docker-compose -f docker-compose.prod.yaml build --no-cache

# Перезапустить
docker-compose -f docker-compose.prod.yaml up -d

# Проверить логи
docker-compose -f docker-compose.prod.yaml logs -f app
```

---

## 🚨 SOS команды (когда всё сломалось)

```bash
# ❌ Приложение не стартует
docker-compose logs app | tail -50

# ❌ БД не работает
docker-compose logs db | tail -50

# ❌ 502 Bad Gateway
docker-compose restart nginx

# ❌ Высокое использование памяти
docker stats
docker system prune -f

# ❌ SSL ошибка
certbot renew
docker-compose restart nginx

# ❌ ПОЛНАЯ ОЧИСТКА (ВНИМАНИЕ!)
docker-compose down -v
docker system prune -f --volumes
docker-compose up -d --build
```

---

## 📞 Где искать информацию

| Вопрос | Где найти |
|--------|----------|
| Как развернуть? | `DEPLOYMENT.md` или `DEPLOY_README.md` |
| Какой хостинг выбрать? | `HOSTING_GUIDE.md` |
| Как устроено приложение? | `ARCHITECTURE.md` |
| Ошибка! Что делать? | `TROUBLESHOOTING.md` |
| Быстрые команды? | Этот файл 😊 |

---

## ✅ Здоровье приложения (проверьте эти URL)

```
https://yourdomain.com/openapi/           ← Swagger UI
https://yourdomain.com/api/profiles       ← API endpoint
https://yourdomain.com/public/            ← Статические файлы
```

**Все 3 работают?** → Поздравляем! ✅ Всё работает! 🎉

---

## 💾 Backup в одну строку

```bash
# Экспресс-бэкап всего
tar -czf backup_$(date +%Y%m%d).tar.gz /app/aviapoint_server/ && docker exec aviapoint-postgres pg_dump -U postgres aviapoint > backup_db_$(date +%Y%m%d).sql
```

---

## 🔄 Обновление кода (CI/CD стиль)

```bash
cd /app/aviapoint_server
git pull origin main
docker-compose -f docker-compose.prod.yaml down
docker-compose -f docker-compose.prod.yaml pull
docker-compose -f docker-compose.prod.yaml build
docker-compose -f docker-compose.prod.yaml up -d
docker-compose -f docker-compose.prod.yaml logs -f app
```

---

## 📊 Мониторинг производительности

```bash
# Текущая нагрузка
docker stats

# CPU топ
docker stats --no-stream | sort -k8 -rh | head

# Память топ
docker stats --format "table {{.Container}}\t{{.MemUsage}}"

# Диск
df -h
du -sh /app/aviapoint_server/*

# БД размер
docker exec aviapoint-postgres du -sh /var/lib/postgresql/data
```

---

## 🌐 Firewall (VPS)

```bash
# Включить
sudo ufw enable

# Основные правила
sudo ufw allow 22/tcp     # SSH
sudo ufw allow 80/tcp     # HTTP
sudo ufw allow 443/tcp    # HTTPS

# Статус
sudo ufw status

# Отключить (только если необходимо!)
sudo ufw disable
```

---

## 🆘 Последняя помощь

Если ничего не помогает:

```bash
# Сохраните логи
docker-compose -f docker-compose.prod.yaml logs > debug.log 2>&1

# Сохраните конфиг
docker-compose -f docker-compose.prod.yaml config > config.yaml

# Сообщите проблему с этими файлами:
# 1. debug.log
# 2. Описание проблемы
# 3. Команды которые вы запускали
# 4. Вывод: docker ps, docker stats, df -h
```

---

## 🎓 Полезные ссылки

- [Docker Docs](https://docs.docker.com/)
- [Dart Guide](https://dart.dev/guides/server)
- [PostgreSQL Docs](https://www.postgresql.org/docs/)
- [Nginx Docs](https://nginx.org/en/docs/)
- [Render.com Docs](https://render.com/docs)

---

**💡 Pro Tip:** Добавьте этот файл в закладки браузера для быстрого доступа!
