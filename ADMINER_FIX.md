# 🔧 Исправление проблемы подключения Adminer к PostgreSQL

## Проблема

Ошибка: `could not translate host name "db" to address`

Adminer не может подключиться к БД, используя имя сервиса `db`.

## Решения

### Решение 1: Использовать имя контейнера (рекомендуется)

В Adminer используйте следующие данные:

- **Система:** `PostgreSQL`
- **Сервер:** `aviapoint-postgres` (имя контейнера) или `db` (имя сервиса)
- **Пользователь:** `postgres`
- **Пароль:** `password` (или из .env)
- **База данных:** `aviapoint`

**Попробуйте сначала `aviapoint-postgres`**, если не работает - используйте `db`.

---

### Решение 2: Проверить, что контейнеры в одной сети

```bash
# На сервере
docker network inspect aviapoint_server_backend | grep -A 5 "Containers"
```

Должны быть оба контейнера: `aviapoint-postgres` и `aviapoint-adminer`

---

### Решение 3: Перезапустить Adminer

```bash
# На сервере
cd /home/aviapoint_server
docker-compose -f docker-compose.prod.yaml restart adminer
```

---

### Решение 4: Использовать IP адрес контейнера БД

```bash
# На сервере - узнать IP адрес контейнера БД
docker inspect aviapoint-postgres | grep IPAddress
```

Затем в Adminer используйте этот IP адрес вместо `db`.

---

## Пошаговая инструкция

1. **Попробуйте подключиться с именем контейнера:**
   - Откройте `http://83.166.246.205:8082`
   - Сервер: `aviapoint-postgres`
   - Остальные данные как обычно

2. **Если не работает, проверьте сеть:**
   ```bash
   docker network ls
   docker network inspect aviapoint_server_backend
   ```

3. **Перезапустите Adminer:**
   ```bash
   docker-compose -f docker-compose.prod.yaml restart adminer
   ```

4. **Попробуйте снова подключиться**

---

## Альтернатива: Подключение через psql

Если Adminer не работает, можно использовать psql напрямую:

```bash
# На сервере
docker exec -it aviapoint-postgres psql -U postgres -d aviapoint
```

---

## Проверка подключения

После исправления попробуйте подключиться с такими данными:

- **Сервер:** `aviapoint-postgres` (попробуйте сначала это)
- **Или:** `db` (если первое не работает)
- **Пользователь:** `postgres`
- **Пароль:** `password`
- **База данных:** `aviapoint`

---

**Готово!** Попробуйте использовать имя контейнера `aviapoint-postgres` вместо `db`! 🔧

