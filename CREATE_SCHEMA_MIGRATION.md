# 📦 Создание и применение миграции структуры БД

## Описание

Создает SQL файл миграции из локальной БД и применяет его на продакшн **без переноса данных** - только структура (таблицы, поля, индексы).

## Шаг 1: Создать миграцию из локальной БД

```bash
# Создать миграцию (автоматически создаст файл с timestamp)
./create_schema_migration.sh

# Или указать имя файла
./create_schema_migration.sh migrations/my_migration.sql
```

**Что делает:**
- Экспортирует структуру БД с локальной машины
- Создает SQL файл миграции
- Исключает таблицу `schema_migrations`
- Обрабатывает файл для безопасного применения

## Шаг 2: Применить миграцию на продакшн

```bash
# Применить созданную миграцию
./apply_schema_migration_to_prod.sh migrations/sync_schema_from_local_YYYYMMDD_HHMMSS.sql

# Или с указанием IP сервера
./apply_schema_migration_to_prod.sh migrations/my_migration.sql 83.166.246.205
```

**Что делает:**
- Копирует SQL файл на продакшн
- Применяет миграцию на продакшн БД
- Игнорирует ошибки для существующих объектов
- Не затрагивает существующие данные

## Полный процесс

```bash
# 1. Создать миграцию
./create_schema_migration.sh

# 2. Проверить созданный файл
cat migrations/sync_schema_from_local_*.sql

# 3. Применить на продакшн
./apply_schema_migration_to_prod.sh migrations/sync_schema_from_local_*.sql
```

## Что включается в миграцию

✅ **Включается:**
- Создание новых таблиц
- Добавление новых полей в существующие таблицы
- Создание новых индексов
- Создание новых ограничений (constraints)
- Изменение типов данных полей

❌ **НЕ включается:**
- Данные (только структура)
- Таблица `schema_migrations`
- Права доступа (owner, privileges)

## Безопасность

✅ **Данные не затрагиваются** - используется `--schema-only`

✅ **Игнорируются ошибки** для существующих объектов - если таблица уже есть, она не будет пересоздана

✅ **Используется транзакция** - если что-то пойдет не так, можно откатить

## Ручное применение через Adminer

Если нужно применить миграцию вручную:

1. Откройте Adminer: `http://83.166.246.205:8082`
2. Войдите в базу данных `aviapoint`
3. Перейдите в раздел **"SQL-запрос"**
4. Скопируйте содержимое файла миграции
5. Выполните запрос

## Проверка после применения

```bash
# Проверить список таблиц на продакшн
ssh root@83.166.246.205
docker exec aviapoint-postgres psql -U postgres -d aviapoint -c "\dt"

# Проверить структуру конкретной таблицы
docker exec aviapoint-postgres psql -U postgres -d aviapoint -c "\d profiles"
```

## Откат изменений

Если нужно откатить миграцию, создайте обратную миграцию или восстановите из резервной копии:

```bash
# Создать резервную копию структуры перед применением
ssh root@83.166.246.205
docker exec aviapoint-postgres pg_dump -U postgres -d aviapoint \
  --schema-only > /tmp/schema_backup_$(date +%Y%m%d_%H%M%S).sql
```

## Примеры использования

### Пример 1: Создать и применить миграцию

```bash
# Создать миграцию
./create_schema_migration.sh

# Найти созданный файл
ls -lh migrations/sync_schema_from_local_*.sql

# Применить на продакшн
./apply_schema_migration_to_prod.sh migrations/sync_schema_from_local_20240101_120000.sql
```

### Пример 2: Создать миграцию с конкретным именем

```bash
# Создать миграцию с именем
./create_schema_migration.sh migrations/add_new_tables.sql

# Применить
./apply_schema_migration_to_prod.sh migrations/add_new_tables.sql
```

## Важные замечания

⚠️ **Всегда проверяйте SQL файл** перед применением на продакшн!

⚠️ **Создавайте резервную копию** перед применением миграций на продакшн

✅ **Данные остаются нетронутыми** - миграция изменяет только структуру

✅ **Безопасно применять несколько раз** - существующие объекты не будут пересозданы


