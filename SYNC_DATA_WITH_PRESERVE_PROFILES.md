# 🔄 Синхронизация данных с сохранением профилей

## Описание

Скрипт синхронизирует данные из локальной БД на продакшн, **сохраняя профили пользователей** и другие пользовательские данные из продакшн.

## Использование

```bash
# Сделать скрипт исполняемым
chmod +x sync_data_with_preserve_profiles.sh

# Запустить скрипт
./sync_data_with_preserve_profiles.sh 83.166.246.205
```

Или без указания IP (используется по умолчанию):

```bash
./sync_data_with_preserve_profiles.sh
```

## Что делает скрипт

### 1. Создает полную резервную копию БД на продакшн
- Экспортирует всю БД с продакшн (структура + данные)
- Сохраняет на сервере: `/tmp/aviapoint_backup_YYYYMMDD_HHMMSS.sql`
- Копирует на локальную машину для дополнительной безопасности
- Можно использовать для полного восстановления БД при необходимости

### 2. Создает резервную копию профилей с продакшн
- Экспортирует таблицу `profiles` с продакшн
- Сохраняет локально для возможного восстановления

### 3. Синхронизирует данные из локальной БД

**Таблицы, которые будут синхронизированы (данные из локальной БД):**
- `aircraft_manufacturers` - производители самолётов
- `aircraft_models` - модели самолётов
- `aircraft_model_specs` - характеристики моделей
- `app_settings` - настройки приложения
- `blog_categories` - категории блога
- `blog_tags` - теги блога
- `subscription_types` - типы подписок
- `airports` - аэропорты
- `hand_book_main_categories` - категории справочника
- `normal_categories` - категории нормальных проверок
- `emergency_categories` - категории аварийных ситуаций
- `preflight_inspection_categories` - категории предполётных проверок
- `preflight_inspection_check_list` - чек-листы предполётных проверок
- `normal_check_list` - чек-листы нормальных проверок
- `rosaviatest_category` - категории тестов Росавиации
- `rosaviatest_questions` - вопросы тестов
- `rosaviatest_answers` - ответы на тесты
- `type_certificates` - типы сертификатов
- `question_type_certificates` - связь вопросов и сертификатов
- `rosaviatest_type_certificates_category` - категории сертификатов
- `type_correct_answers` - правильные ответы

### 4. Сохраняет данные из продакшн (НЕ трогает)

**Таблицы, которые останутся нетронутыми:**
- `profiles` - профили пользователей ⭐
- `flights` - полёты
- `bookings` - бронирования
- `reviews` - отзывы
- `airport_reviews` - отзывы об аэропортах
- `airport_feedback` - обратная связь по аэропортам
- `airport_ownership_requests` - заявки на владение аэропортами
- `flight_photos` - фотографии полётов
- `flight_waypoints` - точки маршрута
- `flight_questions` - вопросы к пилоту
- `blog_articles` - статьи блога
- `blog_article_tags` - теги статей
- `blog_comments` - комментарии к статьям
- `payments` - платежи
- `subscriptions` - подписки
- `stories` - истории
- `news` - новости
- `category_news` - категории новостей
- `video` - видео
- `feedback` - обратная связь
- `airport_visitor_photos` - фотографии посетителей аэропортов

### 5. Проверяет сохранность профилей
- Сравнивает количество профилей до и после синхронизации
- Автоматически восстанавливает из резервной копии, если что-то пошло не так

## Безопасность

✅ **Полная резервная копия БД** - создается полная резервная копия всей БД перед синхронизацией

✅ **Профили защищены** - создается отдельная резервная копия профилей перед синхронизацией

✅ **Автоматическое восстановление** - если профили были затронуты, они восстанавливаются автоматически

✅ **Пользовательские данные сохранены** - все данные пользователей (полёты, бронирования, отзывы и т.д.) остаются нетронутыми

✅ **Двойное резервное копирование** - резервная копия сохраняется и на сервере, и локально

## Процесс синхронизации

1. **Полная резервная копия БД** - экспорт всей БД с продакшн (структура + данные)
2. **Резервная копия профилей** - экспорт таблицы profiles с продакшн
3. **Экспорт данных** - экспорт таблиц из локальной БД
4. **Очистка таблиц** - очистка синхронизируемых таблиц на продакшн
5. **Импорт данных** - импорт данных из локальной БД
6. **Проверка** - проверка сохранности профилей
7. **Восстановление** - автоматическое восстановление, если необходимо
8. **Сохранение резервной копии** - предложение сохранить резервную копию локально

## Пример использования

```bash
# 1. Убедитесь, что локальная БД содержит нужные данные
docker exec aviapoint-postgres psql -U postgres -d aviapoint -c "SELECT COUNT(*) FROM aircraft_manufacturers;"

# 2. Запустите синхронизацию
./sync_data_with_preserve_profiles.sh

# 3. Проверьте результат на продакшн
ssh root@83.166.246.205
docker exec aviapoint-postgres psql -U postgres -d aviapoint -c "SELECT COUNT(*) FROM aircraft_manufacturers;"
docker exec aviapoint-postgres psql -U postgres -d aviapoint -c "SELECT COUNT(*) FROM profiles;"
```

## Важные замечания

⚠️ **Это заменит данные на продакшн!** Убедитесь, что:
- Локальная БД содержит актуальные данные для синхронизации
- Вы понимаете, какие таблицы будут затронуты
- У вас есть резервная копия продакшн БД

✅ **Профили защищены** - скрипт автоматически создает резервную копию и проверяет сохранность

✅ **Безопасно для продакшн** - пользовательские данные (полёты, бронирования, отзывы) не затрагиваются

## Добавление новых таблиц для синхронизации

Если нужно добавить новую таблицу для синхронизации, отредактируйте скрипт:

```bash
# Добавьте таблицу в массив SYNC_TABLES
SYNC_TABLES=(
    "aircraft_manufacturers"
    "aircraft_models"
    "your_new_table"  # <-- добавьте здесь
)
```

## Откат изменений

Если что-то пошло не так, можно восстановить из резервной копии:

### Полное восстановление БД

```bash
# Восстановление из полной резервной копии БД
# На сервере
ssh root@83.166.246.205
cat /tmp/aviapoint_backup_YYYYMMDD_HHMMSS.sql | docker exec -i aviapoint-postgres psql -U postgres

# Или с локальной машины
cat backups/aviapoint_backup_YYYYMMDD_HHMMSS.sql | ssh root@83.166.246.205 "docker exec -i aviapoint-postgres psql -U postgres"
```

### Восстановление только профилей

```bash
# Резервная копия профилей создается автоматически
# Восстановление профилей (если необходимо)
scp root@83.166.246.205:/tmp/profiles_backup.sql ./
cat profiles_backup.sql | docker exec -i aviapoint-postgres psql -U postgres -d aviapoint
```

### Восстановление конкретной таблицы

```bash
# Если нужно восстановить только одну таблицу из полной резервной копии
# 1. Найти нужную таблицу в резервной копии
grep -A 100 "CREATE TABLE.*your_table_name" backups/aviapoint_backup_YYYYMMDD_HHMMSS.sql > restore_table.sql

# 2. Применить на сервере
scp restore_table.sql root@83.166.246.205:/tmp/
ssh root@83.166.246.205 "cat /tmp/restore_table.sql | docker exec -i aviapoint-postgres psql -U postgres -d aviapoint"
```

## Комбинированный подход

Для полной синхронизации (структура + данные):

```bash
# 1. Синхронизировать структуру БД
./sync_schema_local_to_prod.sh

# 2. Синхронизировать данные с сохранением профилей
./sync_data_with_preserve_profiles.sh
```
