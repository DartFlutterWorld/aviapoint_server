# 🔧 Исправление ошибок в миграции

## Проблема

При применении миграции возникли синтаксические ошибки:
```
ERROR:  syntax error at or near "public"
LINE 1: DROP TABLE IF EXISTS CASCADE public.video;
```

## Причина

Команды `DROP TABLE` имеют неправильный синтаксис. `pg_dump` с флагом `--clean` создает команды вида:
```sql
DROP TABLE IF EXISTS CASCADE public.table;
```

Но правильный синтаксис:
```sql
DROP TABLE IF EXISTS public.table CASCADE;
```

## Решение

### Вариант 1: Исправить существующий файл миграции

```bash
# Исправить файл миграции
./fix_schema_migration.sh migrations/sync_schema_from_local_*.sql
```

Этот скрипт:
- Удалит все команды `DROP TABLE` (они не нужны для добавления структуры)
- Создаст резервную копию оригинального файла

### Вариант 2: Создать новую миграцию (исправленный скрипт)

Скрипт `create_schema_migration.sh` уже исправлен и больше не будет создавать проблемные команды `DROP TABLE`:

```bash
# Создать новую миграцию
./create_schema_migration.sh
```

## Что было исправлено

1. ✅ Убран флаг `--clean` из `pg_dump` (он создавал команды DROP)
2. ✅ Добавлена фильтрация всех команд `DROP TABLE` из миграции
3. ✅ Создан скрипт `fix_schema_migration.sh` для исправления существующих файлов

## Применение исправленной миграции

После исправления файла миграции:

```bash
# Применить исправленную миграцию
./apply_schema_migration_to_prod.sh migrations/sync_schema_from_local_*.sql
```

## Важно

⚠️ **Команды DROP TABLE удалены** - это безопасно, так как:
- Мы только добавляем структуру (CREATE TABLE, ALTER TABLE)
- Существующие таблицы не будут пересозданы
- Новые таблицы будут созданы с помощью `CREATE TABLE IF NOT EXISTS`

✅ **Данные не затрагиваются** - миграция изменяет только структуру


