# Переключение между локальной и удалённой БД

## 🚀 Быстрый старт

### Запуск с **ЛОКАЛЬНОЙ** базой данных:
```bash
dart run --define ENVIRONMENT=local
```

### Запуск с **УДАЛЁННОЙ** базой на VPS:
```bash
dart run --define ENVIRONMENT=remote
```

---

## 📋 Как это работает

Конфигурация хранится в `lib/core/config/config.dart`:

- **ЛОКАЛЬНАЯ**: `localhost` с паролем `password`
- **УДАЛЁННАЯ (VPS)**: `83.166.246.205` с паролем `Metra1983@`

### Переменные в коде

```dart
// Проверка текущего окружения
if (Config.isLocal) {
  print('Работаем с локальной БД');
}

if (Config.isRemote) {
  print('Работаем с VPS');
}

// Получить текущее окружение
print(Config.environment); // 'local' или 'remote'
print(Config.dbHost);      // 'localhost' или '83.166.246.205'
```

---

## 🔄 Переопределение через переменные окружения

Можно переопределить хост и пароль через env переменные:

```bash
# Локальная БД с другим хостом
POSTGRESQL_HOST=192.168.1.100 dart run --define ENVIRONMENT=local

# Удалённая БД с другим паролем
POSTGRESQL_PASSWORD=mynewpass dart run --define ENVIRONMENT=remote
```

---

## 💾 Переменные среды в System

### macOS / Linux:
```bash
export ENVIRONMENT=local
export POSTGRESQL_HOST=localhost
export POSTGRESQL_PASSWORD=password

dart run
```

### Windows (PowerShell):
```powershell
$env:ENVIRONMENT = "local"
$env:POSTGRESQL_HOST = "localhost"
$env:POSTGRESQL_PASSWORD = "password"

dart run
```

---

## 📝 Логирование при старте

При запуске приложение выведет:
```
[INFO] Environment: local (Host: localhost)
```
или
```
[INFO] Environment: remote (Host: 83.166.246.205)
```

Это поможет быстро проверить, какое окружение используется.

---

## 🎯 Рекомендации

1. **Разработка**: используйте `--define ENVIRONMENT=local`
2. **Тестирование**: используйте `--define ENVIRONMENT=remote`
3. **Production**: используйте переменные окружения сервера (Docker, VPS)
