# 🔍 Проверка доступности Adminer на сервере

## Способ 1: Проверка статуса контейнера

```bash
# На сервере
cd /home/aviapoint_server

# Проверить статус контейнера Adminer
docker-compose -f docker-compose.prod.yaml ps adminer

# Или проверить все контейнеры
docker-compose -f docker-compose.prod.yaml ps
```

**Ожидаемый результат:**
```
NAME                    COMMAND                  STATUS
aviapoint-adminer       "entrypoint.sh php -S"   Up X seconds
```

---

## Способ 2: Проверка через HTTP запрос

```bash
# Проверить, отвечает ли Adminer на порту 8082
curl -I http://localhost:8082

# Или с полным ответом
curl http://localhost:8082
```

**Ожидаемый результат:**
```
HTTP/1.1 200 OK
...
```

Если получаете ошибку `Connection refused` или `timeout` - Adminer не запущен или порт не проброшен.

---

## Способ 3: Проверка логов контейнера

```bash
# Посмотреть логи Adminer
docker-compose -f docker-compose.prod.yaml logs adminer

# Или последние 50 строк
docker-compose -f docker-compose.prod.yaml logs --tail=50 adminer

# Следить за логами в реальном времени
docker-compose -f docker-compose.prod.yaml logs -f adminer
```

**Ожидаемый результат:**
Логи должны показывать, что PHP сервер запущен и слушает порт 8080 (внутри контейнера).

---

## Способ 4: Проверка порта на сервере

```bash
# Проверить, слушает ли порт 8082
netstat -tuln | grep 8082

# Или через ss
ss -tuln | grep 8082

# Или через lsof (если установлен)
lsof -i :8082
```

**Ожидаемый результат:**
```
tcp6  0  0 :::8082  :::*  LISTEN
```

---

## Способ 5: Проверка из браузера

Откройте в браузере:
```
http://83.166.246.205:8082
```

Если видите форму входа Adminer - всё работает! ✅

---

## Способ 6: Проверка через docker напрямую

```bash
# Проверить статус контейнера
docker ps | grep adminer

# Проверить, запущен ли контейнер
docker ps -a | grep adminer

# Проверить логи
docker logs aviapoint-adminer

# Проверить, слушает ли контейнер порт
docker port aviapoint-adminer
```

**Ожидаемый результат:**
```
CONTAINER ID   IMAGE     STATUS         PORTS
xxxxx          adminer   Up X minutes   0.0.0.0:8082->8080/tcp
```

---

## Если Adminer не запущен

### Запустить Adminer:

```bash
cd /home/aviapoint_server
docker-compose -f docker-compose.prod.yaml up -d adminer
```

### Проверить, что запустился:

```bash
docker-compose -f docker-compose.prod.yaml ps adminer
```

---

## Быстрая проверка (все в одном):

```bash
# На сервере выполните:
cd /home/aviapoint_server && \
echo "=== Статус контейнера ===" && \
docker-compose -f docker-compose.prod.yaml ps adminer && \
echo "" && \
echo "=== Проверка порта ===" && \
curl -I http://localhost:8082 2>&1 | head -1 && \
echo "" && \
echo "=== Проверка через netstat ===" && \
netstat -tuln | grep 8082 || echo "Порт 8082 не слушается"
```

---

## Типичные проблемы:

### 1. Контейнер не запущен
**Решение:**
```bash
docker-compose -f docker-compose.prod.yaml up -d adminer
```

### 2. Порт занят другим процессом
**Проверка:**
```bash
sudo lsof -i :8082
```
**Решение:** Остановите процесс, занимающий порт, или измените порт в `docker-compose.prod.yaml`

### 3. Файрвол блокирует порт
**Проверка:**
```bash
sudo ufw status
```
**Решение:** Откройте порт:
```bash
sudo ufw allow 8082/tcp
```

### 4. Контейнер падает при запуске
**Проверка:**
```bash
docker-compose -f docker-compose.prod.yaml logs adminer
```
**Решение:** Исправьте ошибку в логах

---

## Проверка извне (с вашего компьютера):

```bash
# Проверить доступность с вашего компьютера
curl -I http://83.166.246.205:8082

# Или через telnet
telnet 83.166.246.205 8082
```

Если не работает извне, но работает на сервере - проблема в файрволе или настройках сети.

---

**Готово!** Теперь вы знаете, как проверить доступность Adminer! 🔍

