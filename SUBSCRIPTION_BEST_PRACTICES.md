# Best Practices: Хранение подписок

## 🎯 Рекомендуемый подход: Комбинированный (Hybrid)

### ✅ Что реализовано

**1. Отдельная таблица `subscriptions`** (нормализация)
- Хранит полную историю подписок
- Поддерживает множественные подписки
- Гибкая структура для расширения
- Аудит и аналитика

**2. Денормализация в `profiles`** (производительность)
- `subscription_end_date` - дата окончания подписки
- `has_active_subscription` - флаг активной подписки
- `subscription_updated_at` - время последнего обновления

### 🔄 Автоматическая синхронизация

Триггер в БД автоматически обновляет поля в `profiles` при изменении `subscriptions`:
- При создании новой подписки
- При обновлении подписки
- При деактивации подписки

## 📊 Преимущества подхода

### 1. Производительность
```sql
-- Быстрая проверка без JOIN
SELECT * FROM profiles WHERE has_active_subscription = true;

-- Вместо медленного:
SELECT p.* FROM profiles p 
JOIN subscriptions s ON s.user_id = p.id 
WHERE s.is_active = true AND s.end_date > NOW();
```

### 2. Гибкость
- История подписок сохраняется в `subscriptions`
- Можно добавить новые поля без изменения профиля
- Легко расширять функционал

### 3. Консистентность
- Триггер гарантирует синхронизацию данных
- Нет риска рассинхронизации
- Автоматическое обновление при изменениях

### 4. Простота использования
```dart
// Быстрая проверка в коде
if (profile.hasActiveSubscription && 
    profile.subscriptionEndDate!.isAfter(DateTime.now())) {
  // Доступ разрешен
}
```

## 🔍 Когда использовать что

### Используйте `profiles.has_active_subscription` для:
- ✅ Быстрой проверки доступа
- ✅ Списков пользователей с подпиской
- ✅ Фильтрации в UI
- ✅ Кэширования на клиенте

### Используйте `subscriptions` для:
- ✅ Детальной информации о подписке
- ✅ Истории подписок
- ✅ Аналитики и отчетов
- ✅ Управления подписками

## 📝 Примеры использования

### Проверка доступа (быстро)
```dart
final profile = await profileRepository.fetchProfileById(userId);
if (profile.hasActiveSubscription && 
    profile.subscriptionEndDate!.isAfter(DateTime.now())) {
  // Доступ разрешен
}
```

### Детальная информация (полная)
```dart
final subscription = await subscriptionRepository.getActiveSubscription(userId);
if (subscription != null && subscription.isCurrentlyActive) {
  // Показать детали: оставшиеся дни, тип подписки и т.д.
}
```

### Обновление профиля при изменении подписки
Триггер автоматически обновит `profiles`, но можно и вручную:
```dart
// После создания подписки
await subscriptionRepository.createSubscription(...);
// Профиль обновится автоматически через триггер
```

## 🚀 Миграция

Выполните миграцию для добавления полей в профиль:
```bash
psql -h localhost -U postgres -d aviapoint -f migrations/add_subscription_fields_to_profiles.sql
```

## ⚠️ Важные моменты

1. **Триггер обязателен** - без него данные рассинхронизируются
2. **Индексы важны** - для быстрого поиска пользователей с подпиской
3. **Периодическая проверка** - можно добавить cron job для проверки консистентности

## 🔄 Альтернативные подходы (не рекомендуются)

### ❌ Только в профиле
- Нет истории подписок
- Сложно расширять
- Потеря данных при обновлении

### ❌ Только отдельная таблица
- Медленные запросы (нужен JOIN)
- Сложнее кэширование
- Больше нагрузка на БД

## ✅ Итог

**Комбинированный подход** - это industry standard для систем подписок:
- Отдельная таблица для нормализации и истории
- Денормализация в профиле для производительности
- Автоматическая синхронизация через триггеры

Этот подход используют:
- Stripe (subscriptions + customer metadata)
- PayPal (billing agreements + user profile)
- AWS (subscriptions + account attributes)

---

**Ваша реализация следует best practices!** 🎉

