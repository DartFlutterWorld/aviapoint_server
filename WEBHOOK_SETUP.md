# Настройка Webhook для ЮKassa

## ✅ Текущая конфигурация

Согласно настройкам в личном кабинете ЮKassa:

- **URL для уведомлений:** `https://avia-point.com/payments/webhook`
- **Выбранные события:**
  - ✅ `payment.succeeded` - Успешный платёж
  - ✅ `payment.waiting_for_capture` - Поступление платежа, который нужно подтвердить
  - ✅ `payment.canceled` - Отмена платежа или ошибка оплаты
  - ✅ `refund.succeeded` - Успешный возврат денег покупателю

## 🔧 Что уже реализовано

### Обработка событий в webhook endpoint

Бэкенд обрабатывает все выбранные события:

1. **`payment.succeeded`** - Обновляет статус платежа на "succeeded" и устанавливает `paid = true`
2. **`payment.canceled`** - Обновляет статус платежа на "canceled" и устанавливает `paid = false`
3. **`payment.waiting_for_capture`** - Обновляет статус платежа на "waiting_for_capture"
4. **`refund.succeeded`** - Логирует информацию о возврате (можно расширить функционал)

### Автоматическое создание записей

Если платеж приходит через webhook, но его еще нет в БД, система:
- Получает полную информацию о платеже из API ЮKassa
- Сохраняет платеж в базу данных
- Обновляет статус

## 📋 Инструкция по настройке

### 1. Проверка URL

Убедитесь, что URL в настройках ЮKassa точно соответствует:
```
https://avia-point.com/payments/webhook
```

⚠️ **Важно:**
- URL должен быть доступен из интернета (не localhost)
- Должен использовать HTTPS (обязательно для продакшена)
- Путь должен точно совпадать: `/payments/webhook`

### 2. Проверка доступности endpoint

Проверьте, что endpoint доступен:

```bash
curl -X POST https://avia-point.com/payments/webhook \
  -H "Content-Type: application/json" \
  -d '{"test": "data"}'
```

Должен вернуть `{"status": "ok"}`

### 3. Настройка в личном кабинете ЮKassa

1. Зайдите в личный кабинет: https://yookassa.ru/my
2. Перейдите в **"Настройки"** → **"Уведомления"**
3. Убедитесь, что URL указан правильно: `https://avia-point.com/payments/webhook`
4. Проверьте, что выбраны все нужные события (как показано на скриншоте)
5. Нажмите **"Сохранить"**

### 4. Тестирование webhook

ЮKassa позволяет отправить тестовое уведомление:

1. В личном кабинете найдите раздел "Уведомления"
2. Нажмите кнопку "Отправить тестовое уведомление"
3. Проверьте логи сервера - должно появиться сообщение о получении webhook

## 🔍 Мониторинг и логирование

Все webhook запросы логируются:

```dart
logger.info('Received webhook from YooKassa: $event');
logger.info('Updating payment status: $paymentId -> $status (paid: $paid)');
```

Проверяйте логи сервера для отслеживания:
- Получения webhook запросов
- Обновления статусов платежей
- Ошибок при обработке

## 🔐 Безопасность

### Рекомендации (можно добавить позже):

1. **Проверка подписи запросов:**
   - ЮKassa отправляет заголовок `X-YooMoney-Signature`
   - Можно проверить подпись для защиты от поддельных запросов

2. **IP-фильтрация:**
   - Ограничить доступ только с IP-адресов ЮKassa

3. **Rate limiting:**
   - Ограничить количество запросов с одного IP

## 📊 Обработка событий

### payment.succeeded
- **Когда:** Платеж успешно завершен
- **Действие:** Обновление статуса на "succeeded", `paid = true`
- **Использование:** Можно активировать подписку, отправить уведомление пользователю

### payment.waiting_for_capture
- **Когда:** Платеж получен, но требует подтверждения (capture)
- **Действие:** Обновление статуса на "waiting_for_capture"
- **Использование:** Требуется подтвердить платеж через API или вручную

### payment.canceled
- **Когда:** Платеж отменен или произошла ошибка
- **Действие:** Обновление статуса на "canceled", `paid = false`
- **Использование:** Можно отправить уведомление пользователю, предложить повторить оплату

### refund.succeeded
- **Когда:** Возврат средств успешно выполнен
- **Действие:** Логирование информации о возврате
- **Использование:** Можно расширить для обновления статуса подписки, отправки уведомлений

## 🐛 Отладка

Если webhook не работает:

1. **Проверьте логи сервера:**
   ```bash
   tail -f server.log | grep webhook
   ```

2. **Проверьте доступность URL:**
   ```bash
   curl -v https://avia-point.com/payments/webhook
   ```

3. **Проверьте настройки в ЮKassa:**
   - URL должен быть правильным
   - События должны быть выбраны
   - Webhook должен быть активен

4. **Проверьте формат ответа:**
   - Webhook должен всегда возвращать `200 OK` с `{"status": "ok"}`
   - Если вернуть ошибку, ЮKassa будет повторять запрос

## ✅ Чек-лист

- [x] URL настроен: `https://avia-point.com/payments/webhook`
- [x] Все события выбраны в настройках
- [x] Endpoint обрабатывает все события
- [x] Логирование настроено
- [ ] Проверена доступность endpoint из интернета
- [ ] Протестировано тестовое уведомление из ЮKassa
- [ ] Проверены логи при реальном платеже
- [ ] (Опционально) Добавлена проверка подписи запросов

---

**Текущая конфигурация корректна!** Все выбранные события обрабатываются бэкендом. ✅

